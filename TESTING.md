# 花酿测试说明

## 命令

```powershell
npm test
```

等价于：

```powershell
node --test tests/*.test.js
```

## 当前覆盖

- 网页卡片文本 HTML 转义与换行展示
- 角色切换时不串用其他角色的聊天记录
- 卡片完整服务端渲染
- 卡片内联脚本语法可编译
- 轻聊卡内容独立滚动、输入区固定在底部及卡片宽高比回归
- 按角色选择最近聊天
- 同角色并发聊天串行化
- 角色来访人格快照、PNG base64 角色卡、内嵌世界书与近期酒馆聊天读取
- 临时 Agent 创建、公开来访会话与独立 AGENTS.md 注入
- 送回酒馆后的会话隐藏、配置移除与重启清理
- session 创建/更新失败回滚、active visitor 防误清理与最新对话预算截断
- 「花酿 · 工作台」首页三入口、`to-hana` / `to-tavern` 两种详情页、角色卡实验室跳转、返回链接和内联脚本语法
- 伙伴出口列表 GET 接口、自动加载、成功后跳转到角色选择页；页面导航兼容 `pluginSurfaceSession` 与旧版 `token` 凭证；带 `.deleted-agent.json` 的历史助手不会进入列表、预览或导出；列表与预览按 Yuan 配置显示对应 Hana 内置头像
- Hana 伙伴导出带自定义/默认头像的 PNG 角色卡、`chara`/`ccv3` 元数据、世界书记忆和旧版导出 JSON 清理
- 当前助手根目录 `pinned.md` 与旧版 `memory/` 记忆目录兼容
- 旧 `/page` 入口转入内嵌酒馆，`/legacy` 保留 Edge 酒馆
- SillyTavern frameguard 允许 Hana 页面 iframe 内嵌
- ST 依赖检测、安装失败冷却、强制重试和并发锁；角色新建/更新保存为 ST 可识别 PNG，旧 JSON 角色生成 PNG 后与 Hana 兼容扫描去重
- JSON/YAML/PNG 角色卡导入、同目录来源卡防覆盖、标准字段/扩展字段/PNG 头像保留
- 多来访者 MVU 工具按 agent/session 定位，身份不明确时拒绝串账；设置和调试工具递归隐藏敏感 Key
- 默认插件类通过实例 `this.ctx` 接入生命周期，入口初始化状态、启动主题同步并在卸载时停止同步；干净安装包在依赖目录尚未安装时也能先加载入口，给自动安装流程留出机会
- 真实小剧场桥接：无界面 Chromium/CDP 连接 ST 前端，真实执行 `SillyTavern.getContext().generate()`；变量读取优先 `Mvu.getMvuData({ type: 'chat' })`，兼容消息变量后备，嵌套变量变化按路径回传；并发排队、失败续接、临时聊天前缀清理和超时路径有测试
- 小剧场过程卡：handoff 与主会话绑定、逐幕过程快照回流、固定测卡与代笔对戏分流、同会话多次提交 FIFO、卡片轮询失败/过期停止、等待超时出口和长内容滚动位置保持
- 代笔对戏状态机：同一隔离会话的 start → turn → waiting → turn、跨会话拒绝、并发推进拒绝、明确结束清理、浏览器运行时 mock 分层调用

当前正式测试集覆盖 132 项自动测试（`npm test`）；除单元测试外，2026-09-07 用真实 Edge 无界面页面连接本地 ST，使用方淮角色卡完成真实 `Generate()` 回复，确认 `<think>` 清洗、临时聊天清理、浏览器 profile 清理和 ST 进程可回收；随后用 4 幕真实长回合复验 v1.1.25，确认不会被 30 秒 CDP 命令上限截断；v1.1.26 补做卡片入口、选中角色名称传递和提交锁定回归；v1.1.29 用沈叙完成真实代笔对戏 start → Generate → end 冒烟，角色回复真实回传且临时聊天清理 1 个文件；v1.1.31 新增三档节奏、checkpoint 安全上限、保持克制硬停和 viewport 高度布局回归。变量读不到时仍诚实回传 `null`/不可用，不把缺失误报为空对象。

## 已完成的实机验证

2026-08-27：v1.0.13 实测创建插件私有 Agent/session；`/card/temp-room` 通过 surface session 鉴权加载；页面按钮发送消息后收到真实 Agent 回复。测试完成后已清理临时 Agent 与验证代码。
2026-09-01：隔离环境调用真实角色来访 route handler，验证首页三种 mode 和伙伴列表 GET；用真实 Hana 伙伴导出 PNG，经 SillyTavern parser 读回 ccv3、头像和 4 条世界书记忆；另用真实 JS-Jimp 路径验证 JPEG 自定义头像可转成 PNG；为现有方淮 JSON 生成 PNG，并由官方 SillyTavern parser 读回 ccv3。所有临时输出已清理。
2026-09-09：用真实沈叙角色卡启动代笔对戏，确认驻留无界面 Chromium、真实 `Generate()`、角色回复和变量回传；结束后清理 1 个 `hanabrew-duet-*` 临时聊天。

待实机：重启 Hana 后打开「花酿 · 小剧场」，确认「代笔对戏」按钮、起手偏好和过程卡渲染；在主对话给一句方向，确认小花只代写一条玩家消息、角色真实回复回到卡片，再换一个方向推进第二轮；点「结束这场对戏」确认临时聊天被清理。
