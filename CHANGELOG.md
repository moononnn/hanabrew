# 更新日志

花酿的版本记录。正式发布以 GitHub Release 为准，这份文件按版本归纳主要变化。

## v1.1.50（2026-09-21）

这一版完成内置 SillyTavern 1.19.0 升级、卡面更新和启动恢复能力，公开发布包同步整理完成。

### 新增与升级

- **内置 SillyTavern 升级**：从 1.18.0 升级到 1.19.0，保留花酿主题、内置扩展、iframe 补丁和外部用户数据。
- **新版卡面**：为「花酿·酒馆」和「花酿·工作台」绑定随包分发的新卡面资源。

### 修复与优化

- 关闭内置第三方扩展的自动更新，避免打包版缺少 Git 元数据时启动弹出 Internal Server Error。
- Hana 重启或内嵌扩展状态丢失后，自动重新登记资源，减少手动恢复操作。
- 移除重复的旧酒馆入口，统一使用「花酿·酒馆」卡片。
- 修正工作台与角色卡体检页的跳转路径，并补上带会话凭证的返回工作台入口。
- 继续收口公开版卡面资源路径，避免宿主回退到默认卡背。

### 工程与发布

- 公开版继续排除轻聊入口、运行时数据、本地开发文档和内置扩展开发文件。
- 136 项自动测试全部通过；发布包将按独立发布工作区生成并进行安装模拟与发布树校验。

## v1.1.35（2026-09-10）

这一版把仓库从 v1.0.2 一口气带到当前线：角色来访、伙伴出口、真实测卡与代笔对戏都是这一批进来的。

### 新增

- **角色卡体检（真实测卡）**：在真实 SillyTavern 前端运行时里执行 `Generate()`，世界书、EJS、MVU 和已安装扩展按酒馆实际流程参与，卡片同步展示逐幕测试台词、角色回复和变量变化，结论回到主对话。
- **代笔对戏**：给出大概方向，按「自由发展 / 慢慢推进 / 保持克制」推进玩家侧台词；普通回合连续留在卡片里，关键剧情、关系变化或越界才停下来等确认；使用独立临时聊天，结束自动清理。
- **角色来访**：把酒馆角色的设定、内嵌世界书和近期对话带到 Hana，创建独立会话；支持多人来访、让 TA 住下来、请 TA 回去、送回酒馆并追加新对话。
- **伙伴出口**：预览伙伴的性格、语言习惯、头像和清洗后的相处记忆，导出成 PNG 角色卡；单向快照，酒馆经历不回写 Hana。
- **卡片中心入口**：「花酿 · 来访」与「花酿 · 体检」两张功能卡，外加说明态话术，方便直接说「帮我测一下这张角色卡」。
- **内置第三方扩展**：随包分发酒馆助手（脚本运行与 MVU 变量能力）和提示词模板（EJS 模板能力），无需用户另外安装。
- **MVU 变量账本**：来访角色可通过工具记录 JSON Patch 变量更新，数值不直接显示，控制宏在正文中清洗。

### 修复与优化

- 真实测卡：修复 Mvu 变量读取（chat 级空壳、消息楼层扫描、实时上下文）；CDP 等待时间跟随整段测试时限；ST 模块切换期间安全读取上下文；浏览器异常退出清理连接；临时聊天清理覆盖角色目录与 chats 根目录。
- 角色来访：头像改为内联 base64，绕开 iframe 鉴权；送回酒馆时生成「来访续章」并带游标去重；注入用户档案并过滤指令式内容；多人并存与入驻的状态迁移；重复入驻防护与提示。
- 内嵌酒馆主题跟随改为静态文件下发，彻底避开与酒馆设置系统的反复覆盖抖动。
- 依赖保障：首次启动自动安装、失败自动重试、官方源失败切镜像源、失败冷却与手动兜底。
- 角色卡统一保存为带元数据的 PNG，兼容酒馆原生角色列表。

### 工程与发布

- `manifest.json` 补上 `minAppVersion`；版本号收敛到 manifest 单一来源，状态页页脚动态读取。
- `npm test` 增加前置哨兵：找不到测试文件时非零退出，避免发布包「空跑 0 测试还绿灯」。
- 卡片标题缩短为「花酿 · 来访」「花酿 · 体检」，避免卡片中心截断。
- README 按当前前端形态重写，接入实机截图。
- 内置第三方扩展只归档运行所需文件，开发文件与 source map 不进包。
- 依赖安装兜底分支去掉 `shell: true`。
- 公开版移除轻聊卡片；发布改走独立工作区（本地开发目录 + 仓库克隆 + 同步脚本）。
- 补齐公开版可独立运行测试所需的依赖声明。

## v1.0.2（2026-08-31）

- 彻底修复复制地址按钮。

## v1.0.1（2026-08-31）

- 修复复制地址按钮。

## v1.0.0（2026-08-07）

- 首次发布：把 SillyTavern 1.18.0 搬进 Hana，提供内嵌酒馆整页、依赖自动安装与角色卡测卡 skill。
